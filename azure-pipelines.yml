# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
- stage: SAS_INITIAL
  jobs:
  - job: START_SAS_PIPELINE
    steps:
    - script: echo SAS SOLUTIONS
      displayName: ' SAS Step POC DevOps'

- stage: DEPLOY_SAS
  jobs:
    - job: PROCESSING_CAS
      steps: 
        - script: |
            echo "Itens basic GitOps para tasks build, test, e deploy."
            echo -e "Server $(hostname -f)"
            df -h
            echo -e
            echo Acesse http://www.sas.com/br
          displayName: 'Server Testing'
